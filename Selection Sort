     for (int i = 0; i < n; ++i)
     {
        int min=arr[i];
        int mini=i;        
        for (int j = i; j < n; ++j)
        {
            if(arr[j]<min){
                min=arr[j];
                mini=j;
            }
        }
        swap(arr[i],arr[mini]);
    }
